services:
  - type: web
    name: fanpulse-worldcup
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_ANTHROPIC_API_KEY
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
