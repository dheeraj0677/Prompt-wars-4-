# FanPulse Environment Variables
# Copy this file to .env and fill in your values

# Anthropic API Key (Optional — app works with mock fallbacks if not set)
# This key is read only by server.js and is never exposed to the browser.
ANTHROPIC_API_KEY=your_anthropic_api_key_here
